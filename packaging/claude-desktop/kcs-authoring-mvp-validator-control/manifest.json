{
  "author": {
    "name": "KCS Authoring MVP"
  },
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=16.0.0"
    }
  },
  "description": "Local Claude Desktop KCS article drafting adapter for KCS Authoring MVP.",
  "display_name": "KCS Authoring",
  "keywords": [
    "kcs",
    "support",
    "mcp",
    "validator",
    "claude-desktop"
  ],
  "long_description": "Starts the bundled KCS Authoring MVP stdio MCP server through an autodetected local runtime: uv first, then python3.11/python3 fallback. Installing this MCPB is the only required Claude Desktop setup step for the default local workflow; no repository path, Claude CLI/Code, API key, or semantic-provider configuration is required. The primary tool is kcs_draft_article for reviewer-only KCS article drafting from operator-provided sanitized ticket text. A small support_get_behavior_instructions compatibility helper exists for legacy Plesk Support prompts and only routes to kcs_draft_article. It performs no Zendesk writes, Help Center publication, Claude Desktop-owned provider calls, Claude CLI/Code dependency, or API-key dependency.",
  "manifest_version": "0.3",
  "name": "kcs-authoring-mvp-validator-control",
  "prompts_generated": false,
  "server": {
    "entry_point": "server/index.js",
    "mcp_config": {
      "args": [
        "${__dirname}/server/index.js"
      ],
      "command": "node",
      "env": {}
    },
    "type": "node"
  },
  "tools": [
    {
      "description": "Register one approved sanitized support-ticket transcript as a repository-local clean ticket file. Use this when Claude Desktop receives a draft-article request with an operator-provided sanitized attachment or long paste and no ticket_ref yet. This is the automatic first step for attachment-based drafting. Pass the complete visible sanitized transcript, even when long, in clean_ticket_text and optionally an opaque ticket_ref; do not pass uploaded filenames, local paths, Claude upload paths, item, item_candidates, aliases, or reference article bodies. A Claude Desktop file card is not a filesystem path: do not inspect upload directories, and use the visible file text as clean_ticket_text. The tool writes clean.ticket.txt under local-data/approved-summaries and returns exact next_arguments for kcs_draft_article.",
      "name": "kcs_register_clean_ticket"
    },
    {
      "description": "Primary KCS authoring tool for sanitized support-ticket article requests. Prefer ticket_ref when a trusted source has saved the cleaned ticket transcript under the repository-local approved-summaries store. For an operator-provided sanitized attachment or long paste with no ticket_ref, call kcs_register_clean_ticket first and then call this tool with the returned next_arguments. Use approved_summary_text only as a fallback for short inline sanitized text when clean-ticket registration is not needed. Do not summarize, redact labeled sections, or pass upload filenames, paths, item, item_candidates, aliases, or reference article bodies. A Claude Desktop file card is not a filesystem path; do not inspect upload directories or ask the operator to re-upload while visible file text is available. If no visible file text is available, report file_content_unavailable and do not draft manually. Python validates the input and owns semantic extraction, decision, rendering, and local bundle output. Successful results return compact status plus local reviewer bundle refs; reviewer-only Zendesk HTML is written to the local bundle and returned inline only when debug=true is used for explicit smoke/debug compatibility. If split_required is returned, call again with only operator_selection_ref and operator_selected_item_ref.",
      "name": "kcs_draft_article"
    },
    {
      "description": "Compatibility helper for legacy Plesk Support behavior-instruction requests. Returns the minimal KCS Authoring route: use kcs_draft_article for sanitized ticket article drafting.",
      "name": "support_get_behavior_instructions"
    }
  ],
  "tools_generated": false,
  "version": "0.1.0"
}
